spring:
  application:
    name: ai-code-helper
  profiles:
    active: local
server:
  port: 8123
langchain4j:
  community:
    dashscope:
      chat-model:
        model-name: qwen-max
        api-key: sk-xxxx
      embedding-model:
        model-name: text-embedding-v4
        api-key: sk-xxxx

